<Page x:Class="GetMoreFit.Pages.SlidePanelPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
       xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
       xmlns:lvc="clr-namespace:LiveCharts;assembly=LiveCharts"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="SlidePanelPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="260"/>
        </Grid.ColumnDefinitions>

        <Border CornerRadius="30" Background="#6699FF" Margin="20">
            <Grid>
                <StackPanel VerticalAlignment="Top">
                    <Button Style="{StaticResource menuButton}" >
                        <Icon:PackIconMaterial Style="{StaticResource menuButtonIcon}"/>
                    </Button>
                    <Separator Background="White" Margin="20 0 10 0" Height="0.8" />
                    <Button Style="{StaticResource menuButton}">
                        <Icon:PackIconMaterial Kind="HomeAccount" Margin="0 10"  Style="{StaticResource menuButtonIcon}" />
                    </Button>
                    <Button Style="{StaticResource menuButton}">
                        <Icon:PackIconMaterial Kind="DockBottom" Style="{StaticResource menuButtonIcon}"/>
                    </Button>
                    <Button Style="{StaticResource menuButton}">
                        <Icon:PackIconMaterial Kind="Finance" Style="{StaticResource menuButtonIcon}"/>
                    </Button>
                    <Button Style="{StaticResource menuButton}" >
                        <Icon:PackIconMaterial Kind="MotionOutline" Style="{StaticResource menuButtonIcon}"/>
                    </Button>
                    <Button Style="{StaticResource menuButton}">
                        <Icon:PackIconMaterial Kind="AlarmMultiple" Style="{StaticResource menuButtonIcon}"/>
                    </Button>
                    <Separator Background="White" Margin="20 550 10 0" Height="0.8" />
                    <Button Style="{StaticResource menuButton}">
                        <Icon:PackIconMaterial Kind="ExitToApp" Style="{StaticResource menuButtonIcon}"/>
                    </Button>
                </StackPanel>
            </Grid>

        </Border>
        <Grid x:Name="ContentGrid" 
      Margin="0">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="ContentTransform" X="0"/>
            </Grid.RenderTransform>
            <Frame x:Name="NewFrame" 
           NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Page>
