<Page x:Class="GetMoreFit.Pages.TestTestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:converters="clr-namespace:GetMoreFit.Convertors"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="TestTestPage">

    <Page.Resources>
        <converters:IndexToNumberConverter x:Key="IndexConverter"/>
    </Page.Resources>

        <Grid Background="White">
        <ListView x:Name="TaskUserLV"
      AlternationCount="10000" 
            HorizontalAlignment="Right"
          VerticalAlignment="Bottom"
          Margin="0,0,523,280"
          Width="600"
          Height="560">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="1"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border Width="555"
                    Height="120"
                    Margin="10 10 0 0"
                    Background="#FEB29B"
                    CornerRadius="20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock 
                                    Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, 
                                                 Path=(ItemsControl.AlternationIndex),
                                                 Converter={StaticResource IndexConverter}}"
                                    FontSize="24"
                                    Margin="20 20 5 0"
                                    Width="30"
                                    VerticalAlignment="Top"/>

                                <!-- Основные данные -->
                                <TextBlock Text="{Binding NameProduct}"
                               FontSize="24"
                               Margin="5 20 20 0"
                               VerticalAlignment="Top"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
